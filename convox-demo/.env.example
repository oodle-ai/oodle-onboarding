# Convox v2 (generation 2) deployment target.
# Copy to .env and adjust for your rack/app, then `make up`.

# Rack to deploy into (org/rack form, or a local rack name). See `convox racks`.
RACK=org-team-i4e/gm-test

# Convox app name (created on first `make up`).
APP=rails-demo
