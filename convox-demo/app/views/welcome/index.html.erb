<div class="welcome">
  <h1>🚀 Rails on Convox</h1>
  <p>This sample Ruby on Rails app is running on a Convox v2 (generation&nbsp;2) rack.</p>

  <dl>
    <dt>Rails env</dt><dd><%= @rails_env %></dd>
    <dt>Rails version</dt><dd><%= @rails_version %></dd>
    <dt>Ruby version</dt><dd><%= @ruby_version %></dd>
    <dt>Container</dt><dd><%= @hostname %></dd>
  </dl>

  <p class="health">Health check: <a href="/up">/up</a></p>
</div>

<style>
  body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
         background: #0f172a; color: #e2e8f0; margin: 0; }
  .welcome { max-width: 640px; margin: 12vh auto; padding: 2rem; }
  h1 { font-size: 2.25rem; margin-bottom: 0.5rem; }
  p { color: #94a3b8; line-height: 1.6; }
  dl { display: grid; grid-template-columns: max-content 1fr; gap: 0.4rem 1.5rem;
       margin: 2rem 0; padding: 1.25rem 1.5rem; background: #1e293b; border-radius: 12px; }
  dt { color: #64748b; }
  dd { margin: 0; font-variant-numeric: tabular-nums; }
  a { color: #38bdf8; }
</style>
